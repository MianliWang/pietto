# Phase 53 Slice 1 Window Functions, Generic Signature Compatibility, And Nullability Foundation Scope Lock v1

## Purpose And Slice Identity

This contract locks Phase 53 Slice 1, Scope, Authority, Phase 53–70 Roadmap, Global Window Keyword, And Activation. Phase 53 is Window Functions, Generic Signature Compatibility, And Nullability Foundation. Slice 1 is a static tests/docs/roadmap slice only. It records current authority and later ownership without implementing a grammar, compiler, backend, public artifact, release, or Rust behavior.

The sixteen-slice route, Phase 54–70 owners, exhaustive POST60 mapping, release train, Rust route, exact Gate 2 scope, validation closure, activation rule, and STOP boundaries are fixed here. Their persistence does not authorize any later slice.

## Trusted Phase 52 Baseline

The trusted `main` / `origin/main` baseline is commit `b8029699ccc51bfa500856155b18e666898cb883`, parent `36e466535d923f708a0201ae15a5708f06f2b1f8`, tree `dc7686d7afcfb00cd41f72d2f7fb69f245edf3c5`, and subject `Complete Phase 52 core type system capability foundation`. Natural `CI / push / main` run `29642838835`, attempt 1, completed successfully with `headSha` exactly equal to the baseline. Its Python 3.12 and 3.13 jobs each reported `6167 passed`, generated inventory 8, goldens 37, package smoke PASS, installed CLI `pietto 0.1.0`, and a depth-one checkout.

Phase 52 is `COMPLETED`. Its private capability facts, deterministic lookup, expression-stage vocabulary including unpopulated `WINDOW`, exact-current signatures and contexts, descriptive aggregate evidence, and non-authority guarantees are prerequisites. They do not provide window compiler legality or authorize a Phase 53 behavior.

## Lifecycle And Conditional Activation

Phase 53 remains `UNSTARTED` throughout Gate 2. Gate 2 may implement only this static Slice 1 contract and its exact authority/reader closure, leaving the result uncommitted and unstaged. Writing this contract, either roadmap, or the static test does not activate Phase 53 and does not complete Slice 1.

Only a separately authorized `Phase 53 Slice 1 Gate 3` may activate the phase. It must stage exactly the Gate 2 result, create exactly one commit, perform exactly one normal push to `main`, and observe the unique natural `CI / push / main`, attempt 1, complete successfully with exact matching `headSha`. After and only after that condition, Phase 53 is `ACTIVE`, Slice 1 is complete, and Slices 2–16 remain separately gated and incomplete. No post-CI status-flip commit is planned or required.

## Static-only Slice 1 Boundary

Slice 1 may add this contract, the Phase 53 master plan, the Phase 53–70 current roadmap, one static test, append-only Reconciliation 4, and the minimum direct reader migrations required for exact validation closure. It may lock the future lowercase `window` keyword policy, private identity direction, generic/nullability direction, ownership, release, Rust, validation, publication, and STOP rules.

Slice 1 performs no Pietto import, parser execution, source compilation, generator, build, package operation, runtime or database action, network action, benchmark, or profiler. It adds no source/runtime behavior. It changes no accepted program, diagnostic, SQL byte, public output, version, workflow, tag, or release state.

## Exact Sixteen-slice Route

1. Scope, Authority, Phase 53–70 Roadmap, Global Window Keyword, And Activation
2. Pietto-native Window Syntax And Contextual Grammar Contract
3. WindowSpec, Extension-compatible WindowFunctionIdentity, And AST Contract
4. Generic Type-variable, Constraint, And Exact Compatibility Foundation
5. Nullability Algebra And Signature Result-formula Foundation
6. Private Window Semantic Carrier, WINDOW Stage, Dependency, And Result Roles
7. row_number Direct-field MVP
8. rank / dense_rank And Peer Semantics
9. percent_rank / cume_dist / ntile
10. Partition Binding, Multi-key Visibility, And Diagnostics
11. Window-local Ordering, Direction, Mandatory-order Policy, And Determinism
12. Generic lag / lead Navigation MVP
13. Grouped-result Ranking, Aggregate-result Inputs, And Bounded Let Visibility
14. Multiple Window Outputs, Final-order Alias, Downstream Schema, And Lineage
15. Window IR, PostgreSQL/private-MySQL Lowering, WINDOW_FUNCTION Facts, And Phase 54–70 Readiness
16. Completion Audit, Status Lock, Dialect, Privacy, And No-authority Closure

The order and titles are exact. No slice is reordered, merged, split, widened, completed, or automatically authorized by this contract.

## Phase 54–70 Unique Ownership

- Phase 54: Local Import / Module / Export Foundation.
- Phase 55: Semantic Package Asset Schema And Deterministic Local Loading.
- Phase 56: Capability Profile Static Schema And Declared Checking.
- Phase 57: PostgreSQL Extension Signature Catalog Foundation.
- Phase 58: Public Explain / Portability / Package Inspection Artifact v1.
- Phase 59: Local Package Graph, Attribution, Provenance, And Lineage.
- Phase 60: Advanced Window Frames And Phase 51–60 Ecosystem / Release Readiness Checkpoint.
- Phase 61: Project IR And Semantic Composition Foundation.
- Phase 62: Relationship, JOIN, Grain, And Fanout-safe Semantics.
- Phase 63: Multi-relation SQL Artifacts, Project Emit-SQL, And QUALIFY Lowering.
- Phase 64: Advanced Generic Types, Coercion, Temporal, Decimal, And Native Mapping.
- Phase 65: Advanced Aggregation And Grouping.
- Phase 66: Advanced Module And Semantic-package Assets.
- Phase 67: Remote Package Manager, Registry, Fetch, Install, Cache, And Trust Boundary.
- Phase 68: Dependency Ranges, Version Solver, Canonical Lockfile, And First Rust Kernel.
- Phase 69: Extension-specific Lowering And Additional Dialect Backend Foundation.
- Phase 70: Public Schema / Lineage / Attribution Expansion, Ecosystem Completion Audit, Rust Migration Decision, And v0.2 Release Readiness.

All twelve stable owner slots are exhaustively reconciled:

| Owner slot | Exact current owner |
| --- | --- |
| `POST60_ADVANCED_AGGREGATION_GROUPING` | Phase 65 |
| `POST60_ADVANCED_TYPE_NATIVE_MAPPING` | Phase 64 |
| `POST60_ADVANCED_WINDOWS` | Phase 53 bounded functions and propagation; Phase 60 frames, named/inherited/value/aggregate windows and advanced expressions; Phase 63 `QUALIFY` lowering |
| `POST60_RELATIONSHIP_JOIN_GRAIN_FANOUT` | Phase 62 |
| `POST60_PROJECT_IR` | Phase 61 |
| `POST60_MULTI_RELATION_SQL` | Phase 63 |
| `POST60_PUBLIC_PROJECT_SCHEMA_LINEAGE_EXPANSION` | Phase 70 |
| `POST60_ADVANCED_MODULE_PACKAGE_ASSETS` | Phase 66 |
| `POST60_REMOTE_PACKAGE_MANAGER` | Phase 67 |
| `POST60_DEPENDENCY_SOLVER_LOCKFILE` | Phase 68 |
| `POST60_ADDITIONAL_DIALECT_BACKENDS` | Phase 69 |
| `POST60_EXTENSION_LOWERING` | Phase 69 |

No slot is anonymous. Catalog is not lowering; registry is not solving; graph is not remote fetch; capability facts are not compiler acceptance; compiler support is not backend support; backend support is not server installation; and private lineage is not public exposure.

## Global Window Keyword Policy

The exact lowercase `window` source spelling is a future globally reserved grammar keyword. Slice 1 locks that policy, but there is no grammar change in Slice 1 and no parser change. Slice 2 alone may implement it under a separate authorization. With the current case-sensitive lexer, `Window` and `WINDOW` remain identifiers unless a later explicit case-policy gate changes that behavior.

The planned compatibility impact is explicit: an external previously accepted lowercase identifier named `window` will become a parse error after the separately authorized grammar migration. No tracked positive Pietto fixture or golden uses that identifier. Slice 2 must provide exact positive-before/negative-after compatibility tests and preserve the existing unsupported-clause test, changing only an exact diagnostic expectation if the parse location necessarily moves.

## Contextual Grammar Boundary

`over`, `partition`, `asc`, `desc`, and future frame words remain contextual where practical. `row_number`, `rank`, `dense_rank`, `percent_rank`, `cume_dist`, `ntile`, `lag`, and `lead` remain semantic catalog identifiers, not grammar keywords. Pietto-native syntax uses inline unnamed window specifications and preserves Phase 60 ownership of named windows and inheritance.

Slice 1 adds no token, rule, accepted syntax, parse tree, AST node, parser diagnostic, or generated byte. Slice 2 must change the combined grammar source and regenerate the exact reviewed generated inventory in one separately authorized bounded pass; handwritten generated edits are forbidden.

## Private Window Function Identity And Catalog Boundary

The future private identity is extension-compatible and equivalent to `WindowFunctionIdentity(namespace, name, role)`. The builtin namespace, exact source name, and explicit `WINDOW_FUNCTION` role form a stable identity; carrier module placement and enum spelling remain the separately gated Slice 3 implementation detail. Ordered catalog construction must be immutable and deterministic.

Identity is not legality. A catalog row cannot accept a call, bind an overload, infer a type or nullability, choose a diagnostic, assign a stage, create dependency or lineage, construct IR, lower SQL, claim portability, install an extension, or expose a public API. Existing procedural semantic checks remain compiler authority until a later exact behavior gate proves otherwise.

## Exact Eight-function Inventory

1. `row_number`
2. `rank`
3. `dense_rank`
4. `percent_rank`
5. `cume_dist`
6. `ntile`
7. `lag`
8. `lead`

Each ordered identity uses namespace `builtin` and role `WINDOW_FUNCTION`.

No ninth function is implied. `first_value`, `last_value`, `nth_value`, aggregates used as windows, extension functions, named-window behavior, frames, and `QUALIFY` remain outside Phase 53's eight-function inventory.

## Bounded Window Behavior Ownership

Phase 53's eventual bounded behavior requires explicit output aliases; optional partition keys; mandatory window-local ordering unless an exact later contract proves an exception; `asc` and `desc`; grouped-result keys and aggregate-result inputs in the approved slice; multiple independent window outputs; ordinary downstream derived fields; and compatible final-order aliases.

Same-select window dependencies, nested window calls, windows in `where`, `group by`, aggregate arguments, or `satisfying`, aggregate-as-window use, frames, named windows/inheritance, `QUALIFY`, arbitrary advanced window expressions, and runtime/database execution are excluded. Phase 60 owns the advanced window subset and Phase 63 owns `QUALIFY` lowering.

## Generic Type-variable And Constraint Inventory

The bounded private inventory contains explicit type variables such as `T`; exact same-type binding; evidence-backed constraint tags `Scalar`, `Comparable`, `Orderable`, and `Numeric`; ordered parameter positions; optional arguments; binding-referenced results; and immutable ordered overload rows.

Every variable occurrence, constraint, argument position, optional/default marker, and result reference must be validated at construction. The inventory creates no public type variable, language syntax, generic declaration, typeclass, trait, row polymorphism, inference variable, or solver.

## Exact Compatibility Overload And Ambiguity Contract

Compatibility is exact and deterministic. A candidate binds repeated `T` occurrences to the same exact logical type and checks only its explicitly evidence-backed constraints. Overloads remain in a stable ordered tuple. Deterministic ordered overload selection preserves declared order, requires one unique exact match, and returns a fail-closed ambiguity or unsupported result rather than selecting by set order, mapping order, heuristic specificity, or backend availability.

There is no implicit coercion, no LUB search, promotion, common-supertype or least-upper-bound search, lossy normalization, catalog-driven acceptance, or backend-driven acceptance. Optional arguments and defaults are explicit signature facts. Result types reference exact bindings; they are not recomputed through an unrelated procedural promotion rule.

## Nullability Formula Inventory

The exact ordered symbolic formula inventory is:

1. `NON_NULL`
2. `NULLABLE`
3. `SAME_AS_ARG(i)`
4. `ANY_NULLABLE(args)`
5. `ALWAYS_NULLABLE`
6. `NULLABLE_IF_DEFAULT_OMITTED`
7. bounded deterministic Boolean composition

These are private signature formulas, not replacements for existing concrete `EffectiveNullability`. Construction validates indices and shape. Evaluation consumes ordered argument facts, preserves unknown, never equates unknown with nullable or SQL `NULL`, and fails closed on malformed or unsupported formulas. The navigation subset may later express input/default-sensitive results without mutating current public carriers.

## Phase 64 Advanced Type Exclusions

Phase 64 exclusively owns coercion, promotion, common-supertype and least-upper-bound search, temporal coercion, DateTime/Time/Interval expansion, Decimal precision fusion and overflow/rounding, `Money/Currency/units`, domains, refinements, and backend-native mapping. Phase 53 cannot acquire any of those behaviors indirectly through a generic tag, overload, formula, capability row, or backend implementation.

`Scalar`, `Comparable`, `Orderable`, and `Numeric` are bounded private constraints backed by exact evidence. They are not a final pairwise compatibility matrix or general trait system. Exact same-type binding in Phase 53 is not partial Phase 64 promotion.

## Compiler Authority Capability Facts And Public Privacy Boundary

Existing procedural semantic validation remains the sole compiler-acceptance authority until an exact later behavior gate proves and authorizes a change. Phase 52 capability lookup remains private descriptive evidence with `Found`, `Absent`, `Unknown`, and `Conflict`; facts cannot accept or reject a window, infer its result, suppress or emit a diagnostic, determine backend lowering, or rescue a failure.

All Phase 53 identity, signature, formula, semantic, project, dependency, lineage, and IR carriers remain private unless a later independently versioned public contract is authorized. Slice 1 changes no public Python API, CLI JSON v1, Semantic Metadata Artifact v1, Project JSON v2, public diagnostic envelope, or public SQL API. Phase 58 and Phase 70 retain their public projection owners.

## Window Stage Dependency Result-role And Lineage Readiness

Phase 52 reserves `WINDOW` in the private `CONSTANT`, `ROW`, `GROUP`, `WINDOW`, `UNKNOWN` stage vocabulary and assigns it to no current expression. Phase 53 Slice 6 later owns legal WINDOW assignment and a distinct private `WINDOW_RESULT` project result role. Stage is separate from relation identity, cardinality, grain, project schema availability, result role, and final query order.

Later authorized slices preserve argument, partition, window-order, relation-input, derived-origin, and result-role dependencies. They preserve source order, reject same-select and nested dependencies, keep concrete/unknown/deferred/blocked availability distinct, and use stable serialized occurrence identities across language boundaries. Slice 1 changes no stage, carrier, dependency, lineage, result role, or project schema.

## PostgreSQL And Private-MySQL Evidence Separation

PostgreSQL and private MySQL require independent fail-closed semantic, IR, renderer, diagnostic, and byte evidence. Support for one does not prove support for the other. Semantic acceptance does not prove backend lowering, catalog identity does not prove either backend, and backend support does not prove server installation or runtime behavior.

Slice 15 owns any approved Window IR plus PostgreSQL/private-MySQL lowering. Slice 1 changes no IR model, builder, SQL renderer, fixture, golden, diagnostic, dialect selection, public API, or output byte.

## Historical Roadmap Preservation

`docs/spec/pietto-roadmap-phase45-60-v1.md` remains byte-identical historical authority. Its older phase titles and readiness wording remain history rather than being rewritten to resemble the current route.

The existing 41,661 bytes of `docs/spec/pietto-active-roadmap-phase51-60-v1.md`, including the current final newline, remain an exact prefix. Reconciliations 1–3 remain byte-identical, unique, and ordered. Historical Phase 50 three-function readiness and POST60 wording remains evidence; it is superseded only through append-only current authority.

## Reconciliation 4 Append-only Contract

Exactly one EOF H3 is appended to the predecessor active roadmap: `### Reconciliation 4 — Phase 52 Completion, Phase 53–70 Current-authority Handoff, Release, And Rust Route`. No earlier reconciliation is edited.

Reconciliation 4 records the verified Phase 52 completion, Phase 53 remaining `UNSTARTED` throughout Gate 2, the exact Gate 3 activation condition, the new sole current authority, exhaustive POST60 mapping, Phase 60's bounded production/checkpoint role, release and Rust tracks, no public-schema mutation, no Slice 1 behavior, and no automatic implementation authorization. Append-only persistence is not activation.

## Phase 53–70 Current Roadmap Authority

`docs/spec/pietto-active-roadmap-phase53-70-v1.md` is the sole current roadmap authority once persisted. It preserves both predecessor roadmaps as historical lineage and makes the Phase 51–60 active roadmap immutable after Reconciliation 4. It uniquely owns Phase 53–70 phase titles and scopes, lifecycle, prerequisites, POST60 reconciliation, release train, Rust route, keyword policy, compatibility, validation, publication, and STOP rules.

The new authority neither rewrites historical bytes nor authorizes implementation. Roadmap text cannot complete a gate, start a phase, mutate a public schema, change a package, or publish a release.

## Release Train Boundary

An optional `TestPyPI/private preview` is considered only after Phase 58 under a separate workflow. Phase 60 Gate 3 must not tag or publish; it also must not upload, sign, or attest. A separate Release 0.1.0 workflow follows Phase 60.

Release Gate 0/1 owns readiness, license/metadata, support matrix, changelog, version decision, artifacts, reproducibility, rollback, publication, signing, provenance, and SBOM planning. Release Gate 2 owns exact authorized edits, reproducible sdist/wheel, installed smoke, clean-environment validation, hashes, and any explicitly authorized TestPyPI action, but no production publication. Release Gate 3 alone owns any exact release commit, tag, GitHub Release, PyPI publication, signatures, and attestations. After Phase 70 the target is a v0.2.0 ecosystem beta, not automatic v1.0.

## Rust Migration Track

No big-bang migration is authorized. Phases 53–60 should establish immutable carriers, stable identities/enums, deterministic serialization, explicit procedures, stable AST/IR ownership, exact diagnostics, differential-testable I/O, no ambient callback registries, and no Python object identity across language boundaries.

`Maintenance Phase 5 — Release Engineering, Performance Baseline, And Rust Migration Readiness` remains after Phase 60. Phase 68 is the preferred first production Rust component. The later preferred order is dependency solver/graph; lineage/dependency algorithms; generic binder/nullability evaluator; IR validation/canonicalization; selected SQL helpers; and parser only after grammar stabilization. Every migration requires a Python reference or frozen corpus, differential tests, deterministic parity, explicit fallback, independent benchmark proof, and no silent divergence. Slice 1 adds no Rust/native infrastructure or behavior.

## Exact Gate 2 Allowlist And Dirty-state Contract

The exact Gate 2 scope is `A4/M7/D0`.

Added:

1. `docs/plan/phase-53-window-functions-generic-signature-nullability-foundation.md`
2. `docs/spec/phase53-window-functions-generic-signature-nullability-scope-lock-v1.md`
3. `docs/spec/pietto-active-roadmap-phase53-70-v1.md`
4. `tests/test_phase53_window_generic_nullability_foundation_scope_lock.py`

Modified:

1. `docs/spec/pietto-active-roadmap-phase51-60-v1.md`
2. `tests/test_phase52_core_type_system_capability_foundation_scope_lock.py`
3. `tests/test_phase52_completion_audit_and_status_lock.py`
4. `tests/test_phase52_aggregate_signature_algebra_facts.py`
5. `tests/test_phase52_expression_stage_clause_capability_facts.py`
6. `tests/test_phase52_parity_privacy_cross_phase_readiness_drift_closure.py`
7. `tests/test_phase52_scalar_function_operator_signature_facts.py`

Deleted: none. The four added paths remain untracked, all seven modified paths remain unstaged, and the index remains empty during Gate 2. Exact status is neither a subset nor a superset contract: any path outside this eleven-path allowlist, any deletion, or any staged entry is a STOP.

## Validation Depth-one CI And Gate 3 Publication Contract

Gate 2 first proves exact `A4/M7/D0`, then performs one and only one write-mode Ruff format command over the seven test paths. Validation order is `uv lock --check`; repository Ruff format-check; Ruff lint; production Pyright; test Pyright; exact Tier 1; exact dirty-worktree Tier 2; `git diff --check`; and final full diff, status, index, and protected-boundary evidence.

The direct authority-reader partition remains exactly 41: active-roadmap readers are `27 = 23 dirty-compatible + 4 clean-only`, and Phase 50 historical readers are `14 = 13 dirty-compatible + 1 clean-only`. The exact Phase 50 clean-only node is `tests/test_phase50_window_function_readiness.py::test_compatibility_guards_protected_surfaces_and_dirty_set_are_locked`; it remains excluded from dirty Tier 1.

Tier 1 is exactly `111 passed, 0 deselected`, composed as `69 + 23 + 13 + 6 = 111`. Tier 2 is exactly `6090 passed, 146 deselected`. The 146-node overlay is exactly 110 files, 18,912 bytes, SHA-256 `988640bed8e5b8b78ebae970d57dabb0a81bb416965430fcd86ecfdfbde1f26c`. The tracked base `TIER2_MANIFEST` block and transformed 140-node base remain byte-identical. Gate 2 does not run unfiltered pytest, `scripts/validate.py`, generated/golden checks, package smoke, a build, import, generator, benchmark, profiler, staging, commit, push, tag, release, or GitHub mutation.

Gate 3 requires separate authorization. It stages exactly the eleven paths, creates one commit, and performs one normal push. The unique natural depth-one `CI / push / main`, attempt 1, must complete successfully at the exact new `headSha`; each Python job must produce exactly `6236 passed` and retain generated count 8, goldens 37, package smoke PASS, and installed CLI `pietto 0.1.0`. Gate 3 performs no local validation, amend, second push, manual dispatch, rerun, cancel, tag, release, or same-gate repair. Failure hands off to a separate read-only CI Repair Gate 1.

## No Grammar Source Runtime Public Schema Version Or Release Change

Slice 1 has no source/runtime behavior, no grammar, no generated source, no parser behavior, no AST, no semantic behavior, no capability production facts, no project model, no dependency/lineage behavior, no IR, no SQL, no diagnostic, no CLI or JSON change, no public Python API change, no source fixture, no golden, no example, no script, no workflow, no dependency, no lockfile, no package metadata, no version change, no runtime/database/network operation, no tag, no release, no publish, no upload, no signing, no attestation, no native extension, and no Rust implementation.

Package version remains `0.1.0`. Current public CLI JSON v1, Semantic Metadata Artifact v1, Project JSON v2, public diagnostic envelope, SQL APIs, and output bytes remain unchanged. Slice 1 records future policies and ownership only.

## Stop Conditions

STOP if any controlling evidence identity, physical repository identity, baseline ref, parent/tree, clean starting state, remote main, natural-CI identity, package version, tag/release absence, protected fingerprint, historical prefix, heading order, route, owner mapping, POST60 closure, selector, overlay, hash topology, inventory arithmetic, exact dirty state, or validation count differs.

STOP if a path outside `A4/M7/D0` changes; the index becomes non-empty; a deletion occurs; grammar, production source, generated, public schema, workflow, package, dependency, runtime, release, or Rust behavior becomes necessary; a broad compatibility bypass, extra deselection, second formatter, destructive cleanup, staging, commit, push, or GitHub mutation is required; a non-routine functional failure appears; or Phase 53 activation would be preclaimed. Preserve the exact uncommitted evidence and return through a separately authorized gate.
