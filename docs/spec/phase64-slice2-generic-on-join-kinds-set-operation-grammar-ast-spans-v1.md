# Phase 64 Slice 2 Generic ON, JOIN Kinds, Set-Operation Grammar, AST And Spans v1

## Authority and scope

Baseline `ac32bd5d8f98c7952f4276f8ba2a8c7bdc332c73`, tree
`fc0a2e100c6115ebf771e8313b8061a2eceb98fa`, parent
`691245e48c387357f700d19b9f7318bbf79f3045`; natural CI `34092606235`,
push/main/attempt 1/success, jobs `101649111830` (3.12) and `101649111641`
(3.13). Gate 0 rebound clean synchronized main, no operation/NUL. The prior
unnumbered repair is completed history; its budgets do not belong to Slice 2.
This Slice implements syntax retention and negative admission only. D01–D08,
N=11 and E01–E12 remain unchanged; Slice 3 is not implemented here.

## AST and direct-reader matrix frozen before implementation

| Construct | AST and locations | Current readers | Parse | Current admission | Positive owner |
| --- | --- | --- | --- | --- | --- |
| M1/M2 INNER/LEFT without ON | Existing TableDef/QueryDef and JoinClause/VIA occurrences | Existing semantic, use-ledger, IR readers | unchanged | existing behavior | existing |
| M3 ON-only | JoinClause.on_clause: distinct JoinOnClause with Expression and span | analyzer, module semantic facts, relationship uses, legacy IR | retained | ERROR and non-concrete; never relationship discovery | 3/4 |
| M4 single VIA + ON | VIA tuple and JoinOnClause retained independently | same direct readers | retained | ERROR/non-concrete; no replacement base AST | 3/4 |
| CROSS/RIGHT/FULL/SEMI/ANTI | Exact added AuthoredJoinKind; original JOIN span | AST builder, admission, use ledger | retained | ERROR/non-concrete before old IR conversion | 5/6 |
| Multi-VIA+ON, CROSS+ON/VIA, new-kind multi-hop | All authored occurrences retained, no implicit grouping | same negative boundary | structurally retained | unsupported; no positive combination selection | 3/5/6 |
| Set relation body | SetRelationDef with existing TABLE/QUERY declaration-kind discriminator; SetOperationBody, operator/optional quantifier spans and SetOperand occurrences | declaration catalog, relation availability, semantic facts/attribution, completion, IR | retained | ERROR/non-concrete; no FROM/SELECT or output factory | 9 |
| Omitted quantifier | None and no quantifier span | same set-body readers | retained | never a default or successful set operation | 9 |

Ordinary TableDef/QueryDef constructors and their SELECT-body fields stay intact.
The alternate complete body uses a sibling SetRelationDef in Script.definitions,
classified into the existing RELATION namespace and TABLE/QUERY identity kinds.
It has no invented FromClause, select_items, operand-zero output, or metadata
side channel. This closed AST variant prevents existing SELECT-only readers
from dereferencing a missing FROM. New set operands are not resolved here.

Set bodies accept one or more authored FROM operand lines so Slice 9 can give
the detailed insufficient-operand diagnostic; zero-operand/empty blocks and
repeated/conflicting quantifiers are syntax errors. No additional query tail,
infix mixing, BY NAME, relational DISTINCT or single-match marker is added.
One optional ON follows VIA steps. Keyword/name rules remain contextual.

The shared private AST-admission helper is justified by current independent
single-file analyzer and Project model/module-fact callers. It creates only
availability diagnostics, never a resolver/type/property graph. New availability
uses PIE-S2334 (ERROR in every mode); existing earlier diagnostics are retained.
Legacy IR keeps JOIN-before-QUALIFY precedence and rejects set definitions with
PIE-I1000. Neither SQL renderer changes. PACKAGE_ROOT stays on its existing
explicit branch; parsed package ASTs do not gain positive Project semantics.

The pre-Slice-2 repair remains binding: exact FROM/JOIN dependency evidence,
cycle members versus affected dependents, partial acyclic schedule, all-owner
ledgers, precise PIE-S2302 identity, zero blocked allocation/replay, and unchanged
acyclic private bytes. No set dependency engine is introduced.

## Path activation ledger

Core paths activated before editing: grammar/Pietto.g4, src/pietto/ast_nodes.py,
src/pietto/ast_builder.py, this contract, the Slice-2 principal, docs/roadmap.md,
docs/status.md, tests/test_active_phase_lifecycle.py, and the sole inventory
owner tests/test_validation_performance_interlude_slice4_validator_static_analysis_stage_optimization.py.
Regeneration uses `make generate-parser`; all eight tracked generated files are
verified, with only changed outputs counted. No hand editing generated bytes.

Preauthorized existing support paths activated:

| Path | Direct negative-only purpose |
| --- | --- |
| src/pietto/semantic/analyzer.py | Classify set declarations and diagnose new syntax without losing other definitions |
| src/pietto/semantic/relations.py | Recognize an unavailable named set input, not an unknown name |
| src/pietto/semantic/relation_schemas.py | Prevent a new JOIN from acquiring a fake single-input row schema |
| src/pietto/_project/model.py | Existing declaration identity classification and LEGACY_FLAT admission |
| src/pietto/_project/module_relation_resolution.py | Retain named set owners with explicit unavailable row state |
| src/pietto/_project/module_semantic_fact_preservation.py | Closed non-concrete set facts and precise JOIN/set diagnostics |
| src/pietto/_project/module_attribution.py | Complete empty/non-concrete set lineage without output identities |
| src/pietto/_project/project_relationship_uses.py | New JOIN modes stop before direct relationship resolution/old kind lowering |
| src/pietto/_project/project_ir_construction.py | Reject forged positive set/JOIN facts at direct construction |
| src/pietto/_project/project_completion.py | Retain set declarations without acquiring set operand dependencies |
| src/pietto/ir/builder.py | Whole-ScriptIR negative admission for set definitions |

Additional existing direct readers activated (3/6):

| Path | Caller / exact variant / change |
| --- | --- |
| src/pietto/_project/module_catalog.py | Project model catalog construction / SetRelationDef / admit its existing declaration identity, not semantics |
| src/pietto/_project/project_query_block.py | build_project_query_block_from_relation / non-concrete set fragment / allow owner bridge to return its typed non-concrete result |
| src/pietto/semantic/window_semantics.py | query-block owner bridge / SetRelationDef TABLE/QUERY discriminator / source occurrence only, no window semantics |

New private module (1/1): src/pietto/_flat_relational_admission.py, used by the
analyzer and Project model/module-fact admission. Production inventory becomes
180; the one principal makes test Python 431 -> 432.
docs/language.md and docs/spec/diagnostics.md are activated for availability.

Historical readers activated (2/12):

| Path | Old claim and replacement |
| --- | --- |
| tests/test_phase62_slice10_authored_join_traversal_syntax_semantic_uses.py | Old exact JOIN fields and new-kind/ON parser rejection; retain old fields/behavior, transfer new parsing to this principal, keep unavailable semantics negative |
| tests/test_validation_performance_interlude_ii_slice4_completion_benchmark_phase64_readiness_assurance.py | Phase-64 syntax absent from live grammar; bind absence to immutable Interlude terminal bb521350, retain live historical members |

Phase-63 completion's inherited-feature assertions already read its immutable
handoff contract and require no change. Further paths must be recorded here
before mutation with their actual dependency, within the separate allowances.

Additional direct reader activation 4/6: src/pietto/_project/module_pure_boundary.py.
The existing Project model immediately serializes module inspection, whose
row-state reason decoder rejects the new RELATIONAL_SYNTAX_UNSUPPORTED value.
Admit that exact non-concrete reason in the existing enum domain; do not alter
the format marker, positive records or old canonical bytes.

Additional direct reader activation 5/6: src/pietto/_project/let_scope_facts.py.
The module semantic-fact builder passes a resolved named set declaration as
the unavailable upstream of an ordinary SELECT body. Widen that input's closed
type and reject any claimed concrete set row before the scalar adapter, rather
than casting the new declaration to an old SELECT/source type. No LET semantics
or positive set schema is added.

Integration correction accounting: batch 1 fixes LEGACY_FLAT availability
projection in model.py; batch 2 completes the SetRelationDef row-fact/environment/
window-closure dispatch in module_relation_resolution.py and
module_semantic_fact_preservation.py; batch 3 closes the module pure decoder's
new blocked reason and zero-SELECT relation case; batch 4 corrects the new pure
combination rule's selector encoding (its existing COMBINATION rule does not
use the when field). Batch 5 corrects a test's unsupported assumption of ordinary
parenthesized multiline continuation: both baseline WHERE and JOIN-local ON use
the same single-logical-line scalar grammar. Positive parenthesized expressions
and existing multiline window/QUALIFY forms stay covered. These are separate
roots, all failed logs retained; counts now 5/12, validator starts 0/4.

Historical reader activation 3/12: tests/test_parser_relations.py assumed exactly
one Definition class per top-level grammar header. Add the set-body variant to
its current union check; this principal independently covers both TABLE/QUERY
headers. The Interlude reader's exact self-import list is removed as repository
shape bookkeeping; its historical grammar check stays isolated and network-free.
The sole lifecycle reader follows Slice 3 NEXT while retaining the repair's
historical handoff statement.

Additional direct reader activation 6/6 and corrective batch 6:
src/pietto/_project/project_phase62_pure_boundary.py. A real
build_project_phase62_inspection call on a new CROSS use failed at its existing
kind decoder. Extend only JOIN_USE kind/reason fields for the new blocked syntax;
retain INNER/LEFT-only binary IR and forbid concrete uses for new kinds. Add
direct inspection and corrupt-positive document checks to the principal. No
format change or positive IR behavior. Counts: 6/12 repairs, 6/6 additional
direct readers, 3/12 historical readers, 0/4 validator starts.
The same observer correction also retains syntax_unsupported in its existing
NON_CONCRETE join-use, JOIN-region and multifact-region projections, which
forward those exact use blockers; other fields do not gain that enum value.

Corrective batch 7 fixes only the new diagnostic-order test oracle: existing
PIE-S2005 warnings precede the duplicate-declaration ERROR by source position.
The test now retains those warnings and verifies the precise earlier ERROR
precedes PIE-S2334. No diagnostic behavior changed. Counts 7/12, validator 0/4.

## Complete candidate review and current evidence

One foreground review followed the complete changed consumer chain, including
the module and Phase-62 pure decoders. The review confirmed exhaustive authored
kind dispatch, separate ON/VIA and set-body evidence, no fake SELECT/from/output,
all-owner EXPLICIT_MODULES ledgers, retained LEGACY_FLAT catalog declarations,
and the unchanged pre-language PACKAGE_ROOT rejection. Temporary barriers are
owned by Slices 3–6/9; no-silent-omission, spans, occurrence identity and cycle
laws remain durable. Set operands remain unbound ASTs in this Slice.

The reviewed new forms cannot create concrete relationship uses or binary IR;
direct non-concrete query-block/IR/inspection paths retain their blockers.
New kind words are admitted only in the Phase-62 authored-use field, and the new
reason only in its negative use/region projections. Earlier private formats and
all successful acyclic paths keep their old meaning. No unrelated production
finding remains open, and no additional architecture or positive semantics was
introduced. Installed Ponytail/code-work guidance was applied by direct review;
no separate review runtime, background agent or additional worktree was run.

Fresh local Python 3.13.13 and 3.12.13 each passed the 189-case principal plus
F01/F02 set. The composed parser/JOIN/QUALIFY/historical-reader run passed 585
checks with one test-oracle issue subsequently fixed by batch 7; the affected
189-case run passed after that correction. Another 506 old window/private
inspection/IR/CLI checks passed. Full native generated/golden/package and final
validator evidence follow in the final report; no historical PASS is counted as
a current run. All failed intermediate logs are preserved outside the repo.

## Actual closure and auxiliary gates

Actual closure is A3/M37/D0, 40 paths: the required contract/principal, one
private admission module, seven changed generated outputs, and the activated
core/support/guide/reader paths above. All preauthorized activated support paths
were needed. Additional direct readers are exactly 6/6; historical readers
exactly 4/12; optional private modules 1/1. Production Python is 179 -> 180,
test Python 431 -> 432. Generated __init__.py remains byte-identical; all eight
tracked generated files passed the native reproducibility check.

`scripts/check_goldens.py` verified 39 fixtures (32 SQL byte-exact, seven JSON
structural); `scripts/package_smoke.py` passed wheel/sdist and installed CLI
checks using the locked normal cache. No golden, toolchain, workflow, package
version, dependency or lockfile was changed. The single final authoritative
validator and exact-head CI outcomes are immutable run evidence reported at
handoff; they are not anticipated PASS assertions embedded in this contract.

## Validator-1 findings and bounded continuation

Authoritative validator start 1 failed: 11742 passed, two failed, total 201.727s.
The failed log and candidate are retained; no unchanged rerun. Corrective batch 8
aligns the module pure relation-reason vocabulary order with the live schema enum.
The Phase-54 vocabulary-parity test is correct and remains unchanged.

Historical reader activation 4/12 and corrective batch 9:
tests/test_phase51_aggregate_grouped_state_duplicate_hardening.py. Its blanket
claim that every schema reason must belong to the legacy SELECT dependency/
lineage enums no longer applies to the alternate set-body module terminal.
Those legacy producers accept only TableDef/QueryDef and do not consume set
row facts. Preserve the complete Phase-51/shared reason conversion assertions,
without asserting that later schema-only reasons acquire legacy graph semantics.
The Slice-2 principal verifies the real set terminal owns the new reason and has
no SELECT facts, dependencies or output; no legacy graph/lineage producer changes.
Counts: 9/12 corrective batches, 4/12 historical readers, 6/6 additional direct
readers, 1/4 validator starts consumed. Remaining work rechecks this finding set
and validates the changed candidate before publication.

Post-repair focused evidence: 258 checks passed, including the unchanged live
vocabulary-parity oracle, Phase-51 shared-reason behavior, Slice-2 principal and
F01/F02. Review confirmed the new reason has no SELECT-graph producer and the
reason-order correction preserves all old relative member orders and bytes.
No additional finding family or path activation was introduced.

## Validation and publication contract

Planned implementation is not a corrective batch. Initial counters: corrective
batches 0/12, authoritative validator starts 0/4. No previous task count is
reused. Preserve failed logs; repair before rerunning a failed candidate.
Run the principal and F01/F02 on both local Python versions, relevant existing
readers, targeted static checks, native generated/golden/package gates, and one
complete foreground review before the authoritative Python-3.13 validator.
Publish one ordinary commit `Add Phase 64 flat relational syntax`, normal
fast-forward push, and natural exact-head attempt-1 CI. Successful CI closes
Slice 2 and hands off Slice 3 NEXT / NOT IMPLEMENTED, without starting it.
