# Phase67 两轮规划与 Q

基线 main e0d02247f1f6410c1715ef22eb3c6e254726a962，tree6d29ba3f832fdc4fd64c18383abdd8c00ad73fc1；
natural push/main36088124495、attempt1、13 jobs success 已 rebind。Phase66/N66=16、Interlude V/3
及 R1 均完成。独立 v4 文件未出现在本次 WSL/附件目录；使用当前用户完整重述与 Phase66 handoff，
不把缺文件当 blocker，也不重新决定已接受架构。

## 第一轮：R A C → B J0 → D E F G → H I S

四条复盘事实：Phase66 有受限 native correspondence 而无产品 Arrow 层；V05 meaning 仍有前提；
R1 的完整报告不证明调度最优；S2/S3 证明必须分开 waiting、生产、关键路径、sum 与环境差异。
三条行动：分类先于 placement 且默认新普通节点不能遗漏；规范覆盖独立于 advisory 历史；提前实测
buffer/lifetime/finite completeness，同时不让 optional dependency 泄漏 core。

使命、当前能力等级、三层接受、must/optional/excluded、预算和最多三风险集中在 brief，避免重复审批文书。
必要 readiness 留在 Phase67；仅真正缺前提的事项按下面的准确 owner/最晚时点延后。

| 延后项 | 前提 | owner／最晚决定点 |
| --- | --- | --- |
| Timestamp/UUID producer meaning witnesses | 上游语义/producer 表示的真实正负证据 | Slice07，首次产品 mapping 入域前 |
| product stream completion binding | canonical contract 与有限批次消费者 | Slice09；IPC承载在Slice12前闭合 |
| 产品 ownership/copy/borrow 实施 | 本 Slice capsule/alias witness；有限 reader | Slice10 入域前 |
| 公开 optional-extra 包装 | 已验证有限产品路径 | Slice13；本 Slice 只隔离实验 |
| executor/事务/取消/backpressure | 已发布 result/binding/batch 合同 | Phase68 开始时；本期不冒充执行成功 |

## 第二轮：J V K L → Q

一次 Q：三层 Slice01验收与 [16行路线](slices.md) 的需求覆盖完整；用户锁定选择保留；依赖有向无环；
本地重任务串行，shared group 不拆域，不加 pools；27路径与默认一次联合 rehearsal 可容纳。
执行性决策属于 IMPLEMENTATION_FREEDOM；private Arrow representation/lifetime 选择由本次真实实验授权决定。
没有新的产品/trust 决策需重问。Q最多3次；出现当前不可解 blocker 才 consolidated HOLD，不伪装 deferral。

CI 一次采用已批准的四分区；不做 A/B。requirements 与 placement 独立，coverage v2 绑定两者；
最终 aggregate 接 read-only Actions history，未满可比样本时输出 INSUFFICIENT_EVIDENCE。
Arrow pin25.0.1 的两官方 wheels/哈希/大小已核验；实际实验前不宣称 capsule、UUID 或 truncation 行为已通过。
实验结果只能细化这里已委派的选择；若无法保留批准目标则 HOLD。

外部参考限三组：pytest/xdist（注册与分发）、GitHub Actions（summary/artifact/history）、
Arrow/PyPI（released wheels、协议与 IPC）。各组的 snapshot、问题、identity、layer、算法/界面、
验证生命周期、成本、采纳/不复制及 owner 集中写入 Slice01；文档不是扩大 Pietto 支持域的权威。


## Q2：实际候选闭合

真实Arrow案例已解决已委派的pin/string/UUID/Decimal/timestamp/ownership/CPU选择；没有改变用户锁定架构。
current requirements→placement→execution→coverage/health链已由real xdist、depth-one与完整rehearsal验证；
历史adapter正向取回与digest/context损坏、duplicate run、2-of-3、job完整性均有可执行控制，首轮main无样本仍须
如实INSUFFICIENT_EVIDENCE。review修正了history身份/成本及named-group owner closure，未引入新产品/trust边界。
27路径/A13/M14/D0保持，未改core/native inputs；full1/2、focused9/12，余量只供已授权必要恢复/CI child。
三层接受与16行DAG不变；Slice02仍需独立执行授权。
