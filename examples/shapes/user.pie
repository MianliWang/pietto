pietto 0.9
mode checked
dialect postgres
encoding utf8

shape User:
    id: UUID not null
    email: Text(max = 255) not null @pii @sensitive
    email_norm: Text nullable derive lower(trim(email))
    age: Int nullable ensure self is null or self between 0 and 130
